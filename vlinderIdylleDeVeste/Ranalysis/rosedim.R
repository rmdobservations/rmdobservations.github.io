# quick function to output some properties of a variable.
# results vary with the variable type. But the list function 
# formats it in a compact way.
# RMD 29 jan 2015 
rosedim <-function(x){
      listx<-list(x)
      str(listx)
}
